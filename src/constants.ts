export const PIXELS_PER_METER = 10; // Pixels per meter
export const GRAVITY = 9.81;
export const GROUND_FRICTION = 0.5; // Multiplier for horizontal velocity on bounce (1 = no friction)
export const BOUNCE_FACTOR = 0.5; // Multiplier for vertical velocity on bounce (0 = no bounce, 1 = perfect bounce)
export const TILT_FACTOR = 1; // How much screenY changes per worldZ (higher value = more 'tilt')
